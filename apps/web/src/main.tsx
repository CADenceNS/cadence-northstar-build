import React from 'react';
import ReactDOM from 'react-dom/client';
import { Sprint7App } from './Sprint7App';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Sprint7App />
  </React.StrictMode>
);
